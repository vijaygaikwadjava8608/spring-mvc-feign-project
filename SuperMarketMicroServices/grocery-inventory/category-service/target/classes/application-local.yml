spring:
  application:
    name: category-service

  datasource:
    url: jdbc:postgresql://localhost:5433/categorydb
    username: postgres
    password: categorypass



  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081
